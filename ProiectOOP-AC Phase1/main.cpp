#include <iostream>
using namespace std;

/**
* Component
* - de baza: Rezistenta, Bobina, Condensator
* - compus
* 
* - operatii
*   -> adunare (care poate fi in serie sau in paralel)
* 
* Circuit
* - contine mai multe componente legate intr-un anumit mod
* - 
* 
* 
* Un model de functionare la care ma gandeam:
* definim componente
* R1 = Rezistenta(10);
* R2 = Rezistenta(20);
* C1 = Condensator(342);
* 
* ((R2 + R1 + C2) * (R3 + ...) + r3) * (R4 + L3)
*/

int main()
{
	
	return 0;

}